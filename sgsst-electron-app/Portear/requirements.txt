# Requisitos de runtime para K+AIR (Portear)
# Solo se incluyen los paquetes que el proyecto REALMENTE importa en sus scripts.
# Para regenerar después de instalar Ollama/GGUF (que ya no requiere transformers/torch).
#
# Reinstalar limpio con:
#   python-embed\python.exe -m pip install -r requirements.txt

# --- Analisis de accidentes y PDFs ---
pandas==2.2.3
numpy>=1.26
openpyxl==3.1.5
pdfplumber==0.11.4
PyMuPDF==1.25.2
Pillow>=10.0

# --- Generacion de documentos (FURAT, investigaciones) ---
docxtpl==0.20.0
python-docx>=1.1.0
Jinja2>=3.1
lxml>=5.0
docxcompose>=1.4.0

# --- Portapapeles y GUI auxiliar ---
pyperclip>=1.8
ttkbootstrap>=1.12

# --- Servidor LLM (proxy Flask -> Ollama) ---
flask>=3.0
